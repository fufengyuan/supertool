[package]
name = "claw-plugins"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = "Plugin system — derived from claw-code plugins crate"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
