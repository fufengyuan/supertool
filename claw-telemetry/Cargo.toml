[package]
name = "telemetry"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = "Telemetry primitives — derived from claw-code telemetry crate"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
