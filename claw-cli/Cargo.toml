[package]
name = "supertool-claw"
version = "0.1.0"
description = "Claw agent LLM client wrapper for SuperTool"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1" }
api = { path = "../claw" }
