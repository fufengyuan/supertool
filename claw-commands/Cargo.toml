[package]
name = "commands"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = "Command handlers — derived from claw-code commands crate"

[dependencies]
plugins = { path = "../claw-plugins" }
runtime = { path = "../claw-runtime" }
serde_json = "1"
